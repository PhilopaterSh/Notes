# **httpx**

> ملخص سريع: httpx أداة سريعة ومرنة لفحص واستكشاف خوادم HTTP/HTTPS — فحص حالة السيرفر، استخراج بيانات DNS/IP/CDN/ASN، التقاط لقطات شاشة، واستخراج محتوى مثل روابط وبريد إلكتروني.

---

## ما هي أداة httpx؟

**httpx** هي أداة سطر أوامر متعددة الاستخدامات تستخدم مكتبة `retryablehttp` لإرسال طلبات HTTP/HTTPS مع إمكانية إعادة المحاولة. تُستخدم في:

- فحص أكواد الحالة (`200, 301, 404, ...`).
    
- قياس زمن الاستجابة.
    
- استخراج بيانات مثل IP، CNAME، ASN، CDN، تقنيات الموقع.
    
- التقاط لقطات شاشة عبر متصفح Headless Chrome.
    
- فلترة النتائج واستخلاص روابط/عناوين بريدية/عناوين IP.
    
- دعم HTTP/2، WebSocket، TLS grabber، وغير ذلك.
    

---

## أهم الخيارات (Flags)

> تحت كل جزء أمثلة عملية قابلة للنسخ واللصق.

### 1) إدخال البيانات (INPUT)

|الخيار|الوصف|
|---|---|
|`-l, -list`|ملف يحتوي على قائمة بالخوادم (مثال: `domains.txt`)|
|`-rr, -request`|ملف يحتوي على طلب HTTP خام (مثال: `request.txt`)|
|`-u, -target`|هدف واحد أو عدة أهداف (مثال: `-u example.com -u test.com`)|

**مثال:**

```bash
httpx -l targets.txt
```

---

### 2) الفحوصات (PROBES)

|الخيار|الوصف|
|---|---|
|`-sc, -status-code`|عرض كود الحالة|
|`-cl, -content-length`|عرض حجم المحتوى بالبايت|
|`-ct, -content-type`|عرض نوع المحتوى|
|`-location`|عرض عنوان التوجيه (redirect)|
|`-favicon`|عرض هاش favicon|
|`-hash`|حساب هاش المحتوى (md5, sha256...)|
|`-jarm`|عرض بصمة JARM|
|`-rt, -response-time`|عرض وقت الاستجابة|
|`-title`|استخراج عنوان الصفحة|
|`-bp, -body-preview`|عرض معاينة جسم الصفحة (الافتراضي: 100 حرف)|
|`-server, -web-server`|عرض اسم الخادم (مثال: nginx, apache)|
|`-td, -tech-detect`|كشف التكنولوجيات المستخدمة|
|`-cdn`|كشف CDN أو WAF|
|`-ip`|عرض عنوان IP للخادم|
|`-cname`|عرض سجلات CNAME|
|`-asn`|عرض معلومات ASN|

**مثال:**

```bash
httpx -u example.com -sc -cl -title -server -ip
```

---

### 3) المتصفح بدون واجهة (HEADLESS)

|الخيار|الوصف|
|---|---|
|`-ss, -screenshot`|التقاط لقطات شاشة للمواقع|
|`-system-chrome`|استخدام Chrome المحلي بدلاً من المتصفح المدمج|
|`-st, -screenshot-timeout`|تحديد وقت الانتظار لالتقاط الشاشة|

**مثال:**

```bash
httpx -u example.com -ss -st 5
```

---

### 4) المطابقات (MATCHERS)

|الخيار|الوصف|
|---|---|
|`-mc, -match-code`|مطابقة كود الحالة (مثال: `-mc 200,302`)|
|`-ms, -match-string`|مطابقة نص محدد في المحتوى|
|`-mr, -match-regex`|مطابقة تعبير منتظم|
|`-mrt, -match-response-time`|مطابقة زمن الاستجابة (مثال: `-mrt '< 1'`)|

**مثال:**

```bash
httpx -l targets.txt -mc 200 -ms "login"
```

---

### 5) الاستخراج (EXTRACTOR)

|الخيار|الوصف|
|---|---|
|`-er, -extract-regex`|استخراج بيانات باستخدام Regex|
|`-ep, -extract-preset`|استخراج مسبق (مثل `urls`, `ip`, `mail`)|

**مثال:**

```bash
httpx -u example.com -ep url,mail
```

---

### 6) المرشحات (FILTERS)

|الخيار|الوصف|
|---|---|
|`-fc, -filter-code`|فلترة حسب كود الحالة|
|`-fs, -filter-string`|فلترة حسب نص في المحتوى|
|`-frt, -filter-response-time`|فلترة حسب زمن الاستجابة|
|`-strip`|إزالة وسوم HTML/XML من النص|

**مثال:**

```bash
httpx -l targets.txt -fc 403,404 -strip
```

---

### 7) معدل الطلبات (RATE-LIMIT)

|الخيار|الوصف|
|---|---|
|`-t, -threads`|عدد الخيوط (الافتراضي: 50)|
|`-rl, -rate-limit`|حد الطلبات في الثانية (الافتراضي: 150)|

**مثال:**

```bash
httpx -l targets.txt -t 100 -rl 100
```

---

### 8) الإخراج (OUTPUT)

|الخيار|الوصف|
|---|---|
|`-o, -output`|حفظ النتائج في ملف|
|`-json`|حفظ بصيغة JSON|
|`-csv`|حفظ بصيغة CSV|
|`-store-response`|حفظ استجابات HTTP كاملة|

**مثال:**

```bash
httpx -l targets.txt -o results.json -json
```

---

### 9) التحديث (UPDATE)

|الخيار|الوصف|
|---|---|
|`-up, -update`|تحديث httpx لآخر إصدار|
|`-duc, -disable-update-check`|تعطيل فحص التحديث التلقائي|

**مثال:**

```bash
httpx -up
```

---

### 10) Debug & Optimization

|الخيار|الوصف|
|---|---|
|`-debug`|عرض تفاصيل الطلبات والاستجابات|
|`-timeout`|تحديد زمن الانتظار (افتراضي: 10s)|
|`-retries`|عدد محاولات إعادة الإرسال|
|`-follow-redirects`|متابعة التحويلات (redirects)|

**مثال:**

```bash
httpx -u example.com -debug -timeout 5 -retries 3
```

---

## أمثلة عملية جاهزة

1. فحص قائمة مواقع وعرض كود الحالة والعنوان:
    

```bash
httpx -l domains.txt -sc -title
```

2. التقاط لقطات شاشة للمواقع التي تحتوي على كلمة "admin":
    

```bash
httpx -l targets.txt -ss -ms "admin"
```

3. استخراج جميع الروابط والبريد الإلكتروني من موقع:
    

```bash
httpx -u example.com -ep url,mail
```

4. فحص مواقع باستخدام 100 خيط وحفظ النتائج في JSON:
    

```bash
httpx -l targets.txt -t 100 -o results.json -json
```

5. فحص مع متابعة التوجهات وعرض وقت الاستجابة:
    

```bash
httpx -l targets.txt -follow-redirects -rt
```

---
## إضافة مفتاح API
🔧 الحل السريع: إضافة مفتاح API
1. اذهب إلى:
 https://cloud.projectdiscovery.io
2. أنشئ حسابًا أو سجل دخولك، ثم انسخ مفتاح الـ API الخاص بك.
3.  لتثبيته بشكل دائم، أضف السطر إلى ملف `~/.bashrc` أو `~/.zshrc`:

```
echo 'export PD_API_KEY="afefd13f-f4ab-4f5e-a4de-bb4b5871673d"' >> ~/.bashrc source ~/.bashrc
```
-----

```bash
 
cat mgm.mo.txt |  httpx -sc -location -favicon -jarm -title -server -td -cdn -ip -cname -asn -ss -st 10 -t 100 -rl 100 -retries 3 
```


```Bash
httpx \                     
  -title \
  -status-code \
  -tech-detect \
  -web-server \
  -ip \
  -cname \
  -threads 150 \
  -timeout 8 \
  -retries 3 \
  -rl 100 \
  -silent \
  -o httpx_results.txt

```
----

## خلاصة سريعة

- httpx قوي لجمع المعلومات، مراقبة الخدمات، ودعم مراحل اختبار الاختراق.
    
- مرن جداً مع خيارات عميقة للاستخراج والفلترة.
    
- مناسب للاستخدام الآلي داخل سكربتات أو خطوط أنابيب (CI/CD) لجمع معلومات المواقع.
    

---

## ملاحظات ونصائح عملية

- راعِ سياسات الاستخدام والقوانين المحلية: لا تفحص أو تستغل مواقع بدون إذن صريح.
    
- عند تشغيل تعداد كبير من الطلبات، احترم حدود الشبكة والموارد (rate-limit).
    
- استخدم خيارات `-timeout` و`-retries` لتقليل الضوضاء الناتجة عن مشاكل الشبكة المؤقتة.
    

---

> إذا أردت: 1) نسخة مُصغّرة للطباعة، 2) نسخة إنجليزية، أو 3) تحويل هذا الملف تلقائياً إلى قالب ملاحظة Obsidian (YAML frontmatter مُخصّص)، أخبرني أي واحد تريده.